require 'spec_helper'

describe NCCConfidence do
  it 'has a version number' do
    expect(NCCConfidence::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
